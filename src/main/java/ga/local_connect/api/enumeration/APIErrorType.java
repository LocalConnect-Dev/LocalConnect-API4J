package ga.local_connect.api.enumeration;

public enum APIErrorType {
    INVALID_NUMBER,

    INVALID_URI,
    INVALID_METHOD,
    INVALID_SESSION_SECRET,

    ENDPOINT_NOT_FOUND,

    USER_NOT_FOUND,
    PROFILE_NOT_FOUND,

    UNKNOWN_ERROR
}
